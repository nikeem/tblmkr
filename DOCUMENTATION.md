# TblMaker - Полная документация

## Оглавление

1. [Обзор проекта](#обзор-проекта)
2. [Архитектура](#архитектура)
3. [Детали реализации](#детали-реализации)
4. [Установка и запуск](#установка-и-запуск)
5. [Использование CLI](#использование-cli)
6. [Веб-интерфейс](#веб-интерфейс)
7. [Формат входных данных](#формат-входных-данных)
8. [Разработка](#разработка)
9. [Под капотом](#под-капотом)

---

## Обзор проекта

**TblMaker** — конвертер текстовых данных в JSON-таблицы для конструктора сайтов Creatium.

### Задача

Автоматизировать процесс создания хоккейных таблиц для сайтов клубов. Вместо ручного копирования данных игроков и форматирования HTML, пользователь загружает текстовый файл и получает готовый JSON для вставки в Creatium.

### Возможности

- ✅ Парсинг табулированных данных из текстовых файлов
- ✅ Автоматическое сокращение амплуа (вратарь → вр., защитник → защ., нападающий → нап.)
- ✅ Генерация HTML таблицы с фиксированными ширинами колонок
- ✅ Обработка тренера в отдельной строке
- ✅ CLI интерфейс для локального использования
- ✅ Веб-интерфейс на Streamlit
- ✅ Скачивание или копирование JSON в буфер обмена

### Стек технологий

- **Python 3.10+** — основной язык
- **Streamlit 1.28+** — веб-интерфейс
- **JSON** — формат выходных данных
- **GitHub** — хостинг кода
- **Streamlit Cloud** — хостинг веб-приложения

---

## Архитектура

### Структура проекта

```
tblmaker/
├── tblmaker.py           # Основной модуль с бизнес-логикой
├── app.py                # Streamlit веб-интерфейс
├── template.json         # Шаблон Creatium
├── extract_template.py   # Утилита для извлечения шаблона
├── requirements.txt      # Зависимости Python
├── README.md             # Краткое описание
├── DOCUMENTATION.md      # Эта документация
└── .gitignore           # Исключения Git
```

### Компоненты

#### 1. tblmaker.py — Core модуль

Основной модуль, содержащий всю бизнес-логику:

```python
parse_input(filepath)      # Чтение из файла
parse_text(text)          # Чтение из памяти (для веба)
abbreviate_role(role)     # Сокращение амплуа
generate_html(rows)       # Генерация HTML таблицы
load_template()           # Загрузка JSON шаблона
save_json()               # Сохранение результата
main()                    # CLI entry point
```

#### 2. app.py — Веб-интерфейс

Streamlit приложение с 4 шагами:
1. Ввод данных (файл или текст)
2. Статистика
3. Предпросмотр таблицы
4. Скачивание/копирование JSON

#### 3. template.json — Шаблон Creatium

JSON-конфигурация виджета Creatium с плейсхолдером для HTML таблицы. Размер: ~9.5KB.

---

## Детали реализации

### 1. Парсинг входных данных

#### Проблема множественных табов

Excel и другие программы могут вставлять несколько табов подряд. Решение:

```python
# Фильтруем пустые элементы
parts = [p.strip() for p in line.split('\t') if p.strip()]
```

**До:** `['35', 'Кульбаков Иван', '', '', 'вратарь', '', '18.09.1996', '', 'Беларусь']`
**После:** `['35', 'Кульбаков Иван', 'вратарь', '18.09.1996', 'Беларусь']`

#### Обработка тренера

Тренер находится в конце файла в особом формате:

```
Главный тренер: Исаков Алексей
```

Логика:
```python
if line.startswith('Главный тренер:'):
    coach_name = line.replace('Главный тренер:', '').strip()
    coach = {'name': coach_name}
    continue
```

### 2. Сокращение амплуа

Функция `abbreviate_role()` использует substring matching:

```python
def abbreviate_role(role: str) -> str:
    role_lower = role.lower()
    if 'вратарь' in role_lower:
        return 'вр.'
    elif 'защитник' in role_lower:
        return 'защ.'
    elif 'нападающий' in role_lower:
        return 'нап.'
    return role
```

**Почему substring matching?** Для обработки вариантов вроде "левый защитник", "нападающий правый фланг".

### 3. Генерация HTML

#### Фиксированные ширины колонок

Creatium требует точные проценты:

```python
widths = ['3.75%', '37.625%', '12.7937%', '26.1958%', '19.5542%']
```

**Почему такие странные числа?** Взято из существующей таблицы Creatium для точного совпадения.

#### Стабильное форматирование

Каждая ячейка имеет одинаковый набор атрибутов:

```html
<td style="text-align: center; vertical-align: middle; width: 3.75%;">35<br></td>
```

Это решает проблему нестабильного форматирования из оригинального кода Creatium.

### 4. JSON шаблон

#### Структура шаблона

```json
{
  "uid": "...",
  "className": "WidgetMetaHtml",
  "data": {
    "embeds": {
      "cont": {
        "html": {
          "children": [
            {
              "type": "Code",
              "code": "PLACEHOLDER_HTML"  ← Сюда вставляется таблица
            }
          ]
        }
      }
    }
  }
}
```

#### Путь к HTML

```python
template['data']['embeds']['cont']['html']['children'][0]['code'] = html_table
```

### 5. Разделение логики для CLI и Web

#### DRY принцип

Вместо дублирования кода парсинга:

```python
def parse_input(filepath: str) -> tuple[list[dict], dict | None]:
    """Парсит из файла (для CLI)."""
    with open(filepath, 'r', encoding='utf-8') as f:
        text = f.read()
    return parse_text(text)

def parse_text(text: str) -> tuple[list[dict], dict | None]:
    """Парсит из памяти (для Web)."""
    # Вся логика здесь
    lines = text.strip().split('\n')
    # ...
```

---

## Установка и запуск

### Вариант 1: CLI (локально)

#### Требования
- Python 3.10 или выше

#### Установка

```bash
# Клонирование репозитория
git clone https://github.com/nikeem/tblmkr.git
cd tblmkr

# Создание файла in.txt с данными
# (см. раздел "Формат входных данных")

# Запуск
python3 tblmaker.py
```

#### Результат

Файл `out.txt` с готовым JSON для Creatium.

---

### Вариант 2: Веб-интерфейс (локально)

#### Требования
- Python 3.10 или выше
- pip (менеджер пакетов Python)

#### Установка

```bash
# Клонирование репозитория
git clone https://github.com/nikeem/tblmkr.git
cd tblmkr

# Создание виртуального окружения (рекомендуется)
python3 -m venv venv
source venv/bin/activate  # на macOS/Linux
# или
venv\Scripts\activate  # на Windows

# Установка зависимостей
pip install -r requirements.txt

# Запуск
streamlit run app.py
```

Приложение откроется в браузере: http://localhost:8501

---

### Вариант 3: Веб-интерфейс (онлайн)

#### Использование

1. Откройте: **https://tblmkr.streamlit.app**
2. Загрузите файл или вставьте текст
3. Скачайте или скопируйте JSON

Ничего устанавливать не нужно!

---

## Использование CLI

### Базовое использование

```bash
python3 tblmaker.py
```

Ожидает файл `in.txt` в текущей директории, создаёт `out.txt`.

### Подготовка файла in.txt

Скопируйте таблицу из Excel:

1. Выделите таблицу в Excel
2. Ctrl+C (копировать)
3. Вставьте в текстовый редактор
4. Сохраните как `in.txt`

**Важно:** Убедитесь, что данные разделены табами, а не пробелами.

### Проверка результата

```bash
# Проверить количество игроков
python3 -c "
import json
with open('out.txt') as f:
    data = json.load(f)
html = data['data']['embeds']['cont']['html']['children'][0]['code']
print(f'Строк в таблице: {html.count(\"<tr>\")}')
"
```

---

## Веб-интерфейс

### Обзор

![Скриншот интерфейса]

4 шага:
1. **Ввод данных** — загрузка файла или вставка текста
2. **Статистика** — количество игроков, тренер
3. **Предпросмотр** — отображение HTML таблицы
4. **Экспорт** — скачивание или копирование JSON

### Шаг 1: Ввод данных

#### Способ А: Загрузка файла

- Нажмите "Browse files"
- Выберите `.txt` файл с данными
- Файл автоматически обработается

#### Способ Б: Вставка текста

- Выберите "Вставить текст"
- Скопируйте данные из Excel (Ctrl+C)
- Вставьте в текстовое поле
- Данные автоматически обработаются

**Совет:** При вставке из Excel табы сохраняются автоматически.

### Шаг 2: Статистика

Отображает:
- Количество игроков
- Наличие тренера

Позволяет убедиться, что данные загружены корректно.

### Шаг 3: Предпросмотр

Показывает HTML таблицу как она будет выглядеть в Creatium.

### Шаг 4: Экспорт

#### Скачать JSON

Нажмите "📥 Скачать JSON" — файл `creatium_table.json` скачается.

#### Скопировать в буфер

1. Нажмите "📋 Скопировать JSON"
2. Откроется окно с JSON текстом
3. Выделите текст (Ctrl+A)
4. Скопируйте (Ctrl+C или Cmd+C)
5. Вставьте в Creatium

---

## Формат входных данных

### Обязательная структура

```
Номер	Фамилия, имя	Амплуа	Дата рождения	Гражданство
35	Кульбаков Иван	вратарь	18.09.1996	Беларусь
72	Костин Денис	вратарь	21.06.1995	Россия
```

**Правила:**
- Первая строка — заголовки (обязательно)
- Данные разделены табами (TAB), не пробелами
- Минимум 5 колонок
- Дата в формате DD.MM.YYYY

### Опционально: Тренер

```
...
Главный тренер: Исаков Алексей
```

Добавляется в последней строке, формируется отдельная строка таблицы с пустыми полями номера и даты рождения.

### Полный пример

```
Номер	Фамилия, имя	Амплуа	Дата рождения	Гражданство
35	Кульбаков Иван	вратарь	18.09.1996	Беларусь
72	Костин Денис	вратарь	21.06.1995	Россия
2	Журавлёв Даниил	защитник	08.04.2000	Россия
6	Конюшков Богдан	защитник	20.12.2002	Россия
7	Летунов Максим	нападающий	20.02.1996	Россия
Главный тренер: Исаков Алексей
```

### Подготовка из Excel

1. **Создайте таблицу в Excel** с колонками:
   - Номер
   - Фамилия, имя
   - Амплуа
   - Дата рождения
   - Гражданство

2. **Добавьте данные игроков**

3. **Скопируйте** (Ctrl+C)
   - Выделите всю таблицу включая заголовки
   - Скопируйте

4. **Вставьте** в txt файл или веб-интерфейс
   - Табы сохранятся автоматически

5. **Добавьте тренера** (если есть)
   - В последнюю строку:
   ```
   Главный тренер: Имя Фамилия
   ```

---

## Разработка

### Локальная разработка

#### Настройка окружения

```bash
# Клонирование
git clone https://github.com/nikeem/tblmkr.git
cd tblmkr

# Виртуальное окружение
python3 -m venv venv
source venv/bin/activate

# Зависимости
pip install -r requirements.txt
```

#### Запуск CLI версии

```bash
python3 tblmaker.py
```

#### Запуск веб-версии

```bash
streamlit run app.py
```

#### Тестирование

```bash
# Тест с примером 1
cp in.txt test.txt && python3 tblmaker.py
# Проверить out.txt

# Тест с примером 2
cp in2.txt in.txt && python3 tblmaker.py
# Проверить out.txt
```

### Структура кода

#### tblmaker.py

```python
# Функции в порядке выполнения:

parse_input()      # Читает файл
    ↓
parse_text()       # Парсит текст
    ↓
abbreviate_role()  # Сокращает амплуа
    ↓
generate_html()    # Генерирует HTML
    ↓
load_template()    # Загружает шаблон
    ↓
save_json()        # Сохраняет результат
```

#### app.py

```python
# Streamlit workflow:

st.file_uploader() / st.text_area()  # Ввод
    ↓
parse_text()                         # Парсинг
    ↓
st.metric() / st.dataframe()         # Статистика
    ↓
generate_html()                      # Генерация
    ↓
st.markdown()                        # Preview
    ↓
st.download_button() / st.popover()  # Экспорт
```

### Извлечение нового шаблона

Если нужно обновить шаблон Creatium:

```bash
# 1. Экспортируйте виджет из Creatium в JSON
# 2. Сохраните как new_template.json
# 3. Запустите:

python3 extract_template.py

# Шаблон будет сохранён в template.json с плейсхолдером
```

### Добавление новых сокращений

Редактируйте `abbreviate_role()` в `tblmaker.py`:

```python
def abbreviate_role(role: str) -> str:
    role_lower = role.lower()
    if 'вратарь' in role_lower:
        return 'вр.'
    elif 'защитник' in role_lower:
        return 'защ.'
    elif 'нападающий' in role_lower:
        return 'нап.'
    # Добавьте новое сокращение:
    elif 'новая_позиция' in role_lower:
        return 'сокр.'
    return role
```

---

## Под капотом

### Обработка ошибок

#### CLI

```python
# Ошибки файла выводятся в stderr
# Статус: exit code 1 при ошибке
```

#### Веб

```python
try:
    rows, coach = parse_text(raw_text)
    # ...
except Exception as e:
    st.error(f"❌ Ошибка: {e}")
    st.info("💡 Проверьте формат данных")
```

### Производительность

- **Парсинг:** O(n) где n — количество строк
- **Генерация HTML:** O(n)
- **Размер JSON:** ~10KB для 30 игроков

### Лимиты

#### Входные данные
- Максимальный размер файла (Streamlit Cloud): 200MB
- Рекомендуемое количество игроков: до 100

#### Выходные данные
- Размер JSON: пропорционален количеству игроков
- ~350 байт на игрока

### Совместимость

#### Python
- Минимум: Python 3.10
- Рекомендуется: Python 3.11+
- Типы: используются type hints (`list[dict]`, `dict | None`)

#### Браузеры (для веб-версии)
- Chrome/Edge 90+
- Firefox 88+
- Safari 14+

---

## FAQ

### Q: Почему таблица выглядит криво в Creatium?

A: Проверьте:
1. Все ли колонки имеют данные
2. Нет ли пустых строк между данными
3. Сохранены ли табы (не пробелы)

### Q: Как изменить ширину колонок?

A: Редактируйте `widths` в `generate_html()`:

```python
widths = ['3.75%', '37.625%', '12.7937%', '26.1958%', '19.5542%']
```

### Q: Можно ли использовать для других видов спорта?

A: Да! Измените:
1. Заголовки в `generate_html()`
2. Сокращения в `abbreviate_role()`
3. Ширину колонок при необходимости

### Q: Как добавить больше колонок?

A: Требуется изменение:
1. `parse_text()` — ожидайте 6+ полей
2. `generate_html()` — добавьте колонку
3. `widths` — добавьте процент
4. Шаблон Creatium — обновите структуру

### Q: Веб-версия не работает оффлайн?

A: Streamlit Cloud требует интернет. Для оффлайн использования:
```bash
pip install -r requirements.txt
streamlit run app.py
```

---

## Лицензия

MIT License — свободно используйте в коммерческих и личных проектах.

---

## Поддержка

- **Issues:** https://github.com/nikeem/tblmkr/issues
- **Дисклеймер:** Проект не связан с Creatium официально

---

## История изменений

- **v1.0** — Initial release, CLI версия
- **v1.1** — Добавлен Streamlit веб-интерфейс
- **v1.2** — Добавлена кнопка копирования в буфер обмена

---

**Автор:** Nikita Emelyanov (@nikeem)
**Создан:** 2026
